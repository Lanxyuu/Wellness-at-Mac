import java.io.*;
import java.awt.*;

public class HighScoreWriterLevel3 {
    private ArrayList<String> names;
    private String file;
    private PrintWriter output;
    
    
    public HighScoreWriter(ArrayList<String> n, String f) {
        file = "./Scores/" + f + ".txt";
        names = n;
    }
    
    //assigns each variable the next corresponding line in the text files
    public void write() {
        try {
            output = new PrintWriter(new FileWriter(file));
            for (int x = 0; x < names.size(); x++) {
                output.println(names.get(x));
            }
            if (names.size() < 10) {
                for (int x = names.size(); x < 10; x++) {
                    output.println("arglebargle");
            }
        }
        output.close();
        catch (IOException e) {
        }
    }
        
    public void clear() {
        try {
            input = new PrintWriter(new FileWriter(file));
            for (int x = 0; x < 10; x++) {
                input.println("arglebargle");
            }
        }
        input.close();
        catch (IOException e) {
        }
    }        
}